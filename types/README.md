# types

Shared type definitions. This is a JavaScript project today; document data
shapes here with JSDoc `@typedef`s (e.g. a `Service` or `AboutPage` typedef)
or migrate to TypeScript and place `.d.ts` files here.
