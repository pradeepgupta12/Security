// Metadata wrapper for the Event Gallery page (a client component).
export const metadata = {
  title: "Event Gallery | JSS Group Security & Facility Management",
  description: "Photos from events, deployments and operations by JSS Group Security & Facility Management Pvt. Ltd.",
};

export default function EventGalleryLayout({ children }) {
  return children;
}
