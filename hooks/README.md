# hooks

Reusable React hooks shared across the app (e.g. `useMediaQuery`,
`useScrollSpy`). Co-locate a hook here when it is used by more than one
component; keep single-use hooks next to their component.
